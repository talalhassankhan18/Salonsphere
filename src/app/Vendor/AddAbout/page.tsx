// page.tsx

import React from 'react';
import AddSalonAbout from './AddSalonAbout'; // Same folder, so relative import

const AddSalonAboutPage: React.FC = () => {
  return (
    <div>
      <AddSalonAbout />
    </div>
  );
};

export default AddSalonAboutPage;
